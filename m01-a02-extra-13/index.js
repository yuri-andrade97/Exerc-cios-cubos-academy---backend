const pi = 3.14;
const r = 2;

//formula C = 2 * pi * r

const perimetroCirculo = 2 * pi * r;

console.log(`O perimetro do circulo é ${perimetroCirculo}`);

//formula ao = pi * r ^ 2 

const areaDoCirculo = pi * Math.pow(r, 2);

console.log(`A area do circulo é de ${areaDoCirculo}`)

